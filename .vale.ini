StylesPath = .vale
MinAlertLevel = suggestion
Vocab = Android, CMake, Dotnet, General, Windows
IgnoredScopes = code, tt, kbd, text.frontmatter

# Treat MDX files as Markdown
[formats]
mdx = md

[*.{md,mdx}]
BasedOnStyles = Vale
TokenIgnores = (`[^`]+`)
